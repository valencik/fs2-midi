## fs2-midi

### Usage

This library is currently available for Scala 3.3+.

To use the latest version, include the following in your `build.sbt`:

```scala
libraryDependencies ++= Seq(
  "pink.fff" %% "fs2-midi" % "@VERSION@"
)
```
